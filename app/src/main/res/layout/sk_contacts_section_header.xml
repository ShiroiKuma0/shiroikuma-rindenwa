<?xml version="1.0" encoding="utf-8"?>
<!--
  shiroikuma-rindenwa fork — a letter heading of the grouped Contacts list, in the sister address
  book's shape (shiroikuma-renrakusaki): an optional full-width band on top, then the section
  letter with its count and fold indicator, underlined to the width of the text.

  Tapping anywhere on the row folds or unfolds that section. Every colour, font, rule and margin
  comes from the UI page at bind time (SkStyler.styleContactSection) — the sizes below are only
  what an unstyled measure pass would use.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sk_section_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical">

    <View
        android:id="@+id/sk_section_divider"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:id="@+id/sk_section_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:baselineAligned="true"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/sk_section_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="21sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/sk_section_fold_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textSize="21sp"
                android:textStyle="bold" />

        </LinearLayout>

        <View
            android:id="@+id/sk_section_underline"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_marginTop="2dp"
            android:importantForAccessibility="no" />

    </LinearLayout>

</LinearLayout>
