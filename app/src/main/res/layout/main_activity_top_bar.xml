<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable
            name="showExtraAction"
            type="Boolean" />
        <variable
            name="extraActionClickListener"
            type="View.OnClickListener" />
        <variable
            name="extraActionIcon"
            type="android.graphics.drawable.Drawable" />
        <variable
            name="extraActionContentDescription"
            type="String" />
        <variable
            name="extraActionEnabled"
            type="Boolean" />
        <variable
            name="viewModel"
            type="org.linphone.ui.main.viewmodel.AbstractMainViewModel" />
    </data>

    <!--
      shiroikuma fork: what is left of the title bar is the controls row — drawer menu on the left,
      search and the screen's own action on the right, all outside the account tabs. The tabs
      themselves live above the sliding pane (see the list fragments) so their line can run the
      full width of the screen, and the tab name that used to be the title ("Contacts" / "Calls" /
      …) moved to the folder tabs in the bottom bar.
    -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/sk_main_top_bar_height"
        android:background="@color/sk_bar_background">

        <androidx.constraintlayout.widget.Group
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:constraint_referenced_ids="drawer_menu, search_toggle"
            android:visibility="@{viewModel.searchBarVisible ? View.GONE : View.VISIBLE}" />

        <androidx.constraintlayout.widget.Group
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:constraint_referenced_ids="cancel_search, search, clear_field"
            android:visibility="@{viewModel.searchBarVisible ? View.VISIBLE : View.GONE, default=gone}" />

        <ImageView
            style="@style/icon_main_top_bar_button_style"
            android:id="@+id/drawer_menu"
            android:onClick="@{() -> viewModel.openDrawerMenu()}"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/sk_main_top_bar_icon_size"
            android:src="@drawable/list"
            android:contentDescription="@string/content_description_open_drawer_menu_icon"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageView
            style="@style/icon_main_top_bar_button_style"
            android:id="@+id/search_toggle"
            android:onClick="@{() -> viewModel.openSearchBar()}"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/sk_main_top_bar_icon_size"
            android:src="@drawable/magnifying_glass"
            android:contentDescription="@string/content_description_open_filter"
            app:layout_constraintEnd_toStartOf="@id/extra_action"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageView
            style="@style/icon_main_top_bar_button_style"
            android:id="@+id/extra_action"
            android:onClick="@{extraActionClickListener}"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/sk_main_top_bar_icon_size"
            android:src="@{extraActionIcon, default=@drawable/dots_three_vertical}"
            android:contentDescription="@{extraActionContentDescription}"
            android:enabled="@{extraActionEnabled}"
            android:visibility="@{!viewModel.searchBarVisible &amp;&amp; showExtraAction ? View.VISIBLE : View.GONE, default=gone}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageView
            style="@style/icon_main_top_bar_button_style"
            android:id="@+id/cancel_search"
            android:onClick="@{() -> viewModel.closeSearchBar()}"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/sk_main_top_bar_icon_size"
            android:src="@drawable/caret_left"
            android:contentDescription="@string/content_description_cancel_filter"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/search"
            app:layout_constraintBottom_toBottomOf="@id/search" />

        <com.google.android.material.textfield.TextInputLayout
            style="?attr/textInputFilledStyle"
            android:id="@+id/search"
            android:layout_width="0dp"
            android:layout_height="@dimen/sk_main_top_bar_icon_size"
            android:gravity="center_vertical"
            android:textColorHint="@color/sk_ink_faint"
            app:hintEnabled="false"
            app:hintAnimationEnabled="false"
            app:hintTextColor="@color/sk_ink_faint"
            app:boxStrokeWidth="1.5dp"
            app:boxStrokeWidthFocused="3dp"
            app:boxStrokeColor="@color/sk_field_border"
            app:layout_constraintEnd_toStartOf="@id/clear_field"
            app:layout_constraintStart_toEndOf="@id/cancel_search"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <com.google.android.material.textfield.TextInputEditText
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:textCursorDrawable="@null"
                android:textColor="?attr/color_main1_500"
                android:textColorHint="@color/sk_ink_faint"
                android:textSize="16sp"
                android:inputType="text"
                android:paddingVertical="1dp"
                android:imeOptions="actionSearch"
                android:text="@={viewModel.searchFilter}"
                android:hint="@string/search"
                android:background="@android:color/transparent" />

        </com.google.android.material.textfield.TextInputLayout>

        <ImageView
            style="@style/icon_main_top_bar_button_style"
            android:id="@+id/clear_field"
            android:onClick="@{() -> viewModel.clearFilter()}"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/sk_main_top_bar_icon_size"
            android:src="@drawable/x"
            android:contentDescription="@string/content_description_clear_filter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/search"
            app:layout_constraintBottom_toBottomOf="@id/search" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
