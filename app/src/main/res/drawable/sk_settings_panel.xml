<?xml version="1.0" encoding="utf-8"?>
<!--
  shiroikuma fork — a settings panel that is not under a foldable header (the CardDAV / LDAP
  editors, the developer page, the secure-mode page). Same yellow box, all four corners rounded.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="15dp" />
    <solid android:color="@color/sk_surface" />
    <stroke android:width="2dp" android:color="@color/sk_yellow" />
</shape>
