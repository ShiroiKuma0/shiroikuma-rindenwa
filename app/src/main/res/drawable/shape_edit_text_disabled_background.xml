<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork: still recognisably a field, but the border is dimmed to read as disabled. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="63dp" />
    <solid android:color="?attr/color_grey_200"/>
    <stroke android:width="1.5dp" android:color="@color/sk_field_border_dim" />
</shape>
