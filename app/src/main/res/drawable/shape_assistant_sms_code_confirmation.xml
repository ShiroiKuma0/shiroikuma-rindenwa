<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork: the SMS-code boxes are fields — yellow border like every other field pill. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="15dp" />
    <size android:width="73dp" android:height="73dp" />
    <solid android:color="?attr/color_main2_000" />
    <stroke android:width="1.5dp" android:color="@color/sk_field_border"/>
</shape>
