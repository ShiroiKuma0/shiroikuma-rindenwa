<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork: the conversation compose field is a field pill too — yellow border. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="63dp" />
    <solid android:color="?attr/color_message_text_field_background"/>
    <stroke android:width="1.5dp" android:color="@color/sk_field_border" />
</shape>
