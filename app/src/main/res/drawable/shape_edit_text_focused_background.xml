<?xml version="1.0" encoding="utf-8"?>
<!-- shiroikuma fork: same yellow border, thicker, so focus still reads as a state change now
     that the unfocused pill is yellow too. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="63dp" />
    <solid android:color="?attr/color_text_field_background"/>
    <stroke android:width="3dp" android:color="@color/sk_field_border" />
</shape>
